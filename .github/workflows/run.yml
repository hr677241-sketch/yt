name: YouTube Auto Pipeline

on:
  workflow_dispatch:
    inputs:
      batch_size:
        description: 'How many items per run'
        required: false
        default: '3'
      speed:
        description: 'Speed (1.05 = 5% faster)'
        required: false
        default: '1.05'
      order:
        description: 'Upload order'
        required: false
        default: 'oldest'
        type: choice
        options:
          - oldest
          - newest

  schedule:
    - cron: '0 */8 * * *'

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 60  # Should be fast now

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull latest
        run: git pull origin main || true

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install System Packages
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg nodejs npm

      - name: Install Python Packages & Plugins
        run: |
          pip install --upgrade pip
          pip install --upgrade yt-dlp
          pip install -r requirements.txt
          
          # Install the PO Token Plugin provider
          npm install -g yt-dlp-get-pot

      - name: Run pipeline
        env:
          SOURCE_URL: ${{ secrets.SOURCE_URL }}
          YOUTUBE_TOKEN: ${{ secrets.YOUTUBE_TOKEN }}
          YOUTUBE_COOKIES_B64: ${{ secrets.YOUTUBE_COOKIES_B64 }}
          YOUTUBE_COOKIES: ${{ secrets.YOUTUBE_COOKIES }}
          BATCH_SIZE: ${{ github.event.inputs.batch_size || '3' }}
          SPEED: ${{ github.event.inputs.speed || '1.05' }}
          PRIVACY: "public"
          ORDER: ${{ github.event.inputs.order || 'oldest' }}
        run: python main.py

      - name: Cleanup
        if: always()
        run: rm -f cookies.txt

      - name: Save history
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add history.txt
          git diff --staged --quiet || git commit -m "uploaded [skip ci]"
          git push || true
